# Convite Kamilly

Projeto Vue 3 com Vite — carta animada que abre e revela um convite.

## Como rodar

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — servidor de desenvolvimento
- `npm run build` — build de produção
- `npm run preview` — preview do build
