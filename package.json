{
  "name": "convite-kamilly",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx -y firebase-tools@latest deploy --only hosting,firestore",
    "deploy:hosting": "npm run build && npx -y firebase-tools@latest deploy --only hosting",
    "report:rsvp": "node scripts/generate-rsvp-report.mjs"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "gsap": "^3.15.0",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "dotenv": "^17.4.2",
    "firebase-admin": "^14.3.0",
    "pdfkit": "^0.20.2",
    "vite": "^8.1.1"
  }
}
